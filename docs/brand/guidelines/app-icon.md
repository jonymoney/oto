# App icon

Approved: **I1** — byakuroku field, shikkoku wordmark, moegi dot.

- `logo/app-icon.svg` — master (1024 viewBox, full-bleed square; iOS applies
  its own mask).
- `logo/app-icon-1024.png` — flattened export for App Store Connect.

Variants:
- Dark-mode alternate (I2 construction): shikkoku field `#181A13`, shironeri
  wordmark `#EEF1E5`, moegi dark dot `#AACF53`. Regenerate from make_logo.py
  with swapped fills if adopted.
- At 16px-class sizes (Spotlight small, watch): the monogram `o.` replaces the
  full wordmark (see mark.svg construction).

Rejected: 音 as primary icon (hidden layer isn't the front door; muddy <16px).
